<%= form_with(model: beer_club, local: true) do |form| %>
  <% if beer_club.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(beer_club.errors.count, "error") %> prohibited this beer_club from being saved:</h2>

      <ul>
      <% beer_club.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field form-group row">
    <%= form.label :name, class:"col-sm-2 col-form-label" %>
    <%= form.text_field :name, class:"col-sm-7" %>
  </div>

  <div class="field form-group row">
    <%= form.label :founded, class:"col-sm-2 col-form-label" %>
    <%= select_year(Time.current.year, start_year: 1040, end_year: Time.current.year, field_name: :founded, prefix: :beer_club, class:"col-sm-7") %>
  </div>

  <div class="field form-group row">
    <%= form.label :city, class:"col-sm-2 col-form-label" %>
    <%= form.text_field :city, class:"col-sm-7" %>
  </div>

  <div class="actions form-group row">
    <%= form.submit class:"btn btn-primary" %>
  </div>
<% end %>
