<%= form_with(model: style, local: true) do |f| %>
  <% if style.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@style.errors.count, "error") %> prohibited rating from being saved:</h2>

      <ul>
      <% style.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group row">
    <%= f.label :name, class:"col-sm-2 col-form-label" %>
    <%= f.text_field :name, id: :style_name, class:"col-sm-7" %>
  </div>
  <div class="field form-group row">
    <%= f.label :description, class:"col-sm-2 col-form-label" %>
    <%= f.text_field :description, id: :description, class:"col-sm-7" %>
  </div>
  <div class="field form-group row">
    <%= f.submit class:"btn btn-primary" %>
  </div>
<% end %>